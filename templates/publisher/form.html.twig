{% extends 'layout.html.twig' %}

{% block content %}
    {% if errors | length > 0 %}
        <ul class="alert alert-danger">
            {% for item in errors %}
                <li>{{ item.message }}</li>
            {% endfor %}
        </ul>
    {% endif %}

    {{ form(publisherForm) }}
{% endblock %}

